{% extends 'base.html.twig' %}

{% block title%}New Task {% endblock %}

{% block body %}
  <div class="my-5">
    <h1>Add New Task</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    {{ form_end (form) }}
  </div>
{% endblock %}